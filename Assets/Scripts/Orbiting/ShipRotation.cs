using UnityEngine;

public class ShipRotation : MonoBehaviour
{
    //Obtain ship transform.forward

    //obtain angle between ship and player in comparision to transform.forward

    //Turn ship by X degrees until transform.forward = shiptoPlayer


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
